import 'package:flutter/material.dart';

const BorderRadiusGeometry borderRadiusGridView = BorderRadius.vertical(
  top: Radius.circular(15),
  bottom: Radius.circular(45),
);

final BorderRadiusGeometry borderRadiusSearch = BorderRadius.circular(60);
final BorderRadiusGeometry borderRadiusBotonMenu = BorderRadius.circular(60);
